<h1>Listing workflows</h1>

<table>
  <thead>
    <tr>
      <th>Subprocess group</th>
      <th>Pmbok doc</th>
      <th>Direction</th>
      <th>Direction subprocess</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @workflows.each do |workflow| %>
      <tr>
        <td><%= workflow.subprocess_group %></td>
        <td><%= workflow.pmbok_doc %></td>
        <td><%= workflow.direction %></td>
        <td><%= workflow.direction_subprocess %></td>
        <td><%= link_to 'Show', workflow %></td>
        <td><%= link_to 'Edit', edit_workflow_path(workflow) %></td>
        <td><%= link_to 'Destroy', workflow, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Workflow', new_workflow_path %>
