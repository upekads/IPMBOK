<%= form_for(@pmbok_doc, html: {class: "form-horizontal"}) do |f| %>
  <% if @pmbok_doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pmbok_doc.errors.count, "error") %> prohibited this pmbok_doc from being saved:</h2>

      <ul>
      <% @pmbok_doc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <div class="col-lg-2"><%= f.label :Name %></div>
      <div class="col-lg-5"><%= f.text_field :name, :class => "form-control" %></div>
    </div>
    <div class="form-group">
      <div class="col-lg-2"><%= f.label :Sub_Process_Group %></div>
      <div class="col-lg-5"> <%= f.collection_select(:subprocess_group_id, SubprocessGroup.all, :id, :name) %></div>


    </div>
    <div class="form-group">
      <div class="col-lg-12 text-left">
        <%= f.submit "Submit", :class => "btn btn-default" ,:disabled =>true%> &nbsp;&nbsp;
        <%= link_to 'Back', pmbok_docs_path, :class => "btn btn-default" %>
      </div>
    </div>
<% end %>
